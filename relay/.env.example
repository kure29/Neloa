# Generate a private value with: openssl rand -hex 32
NELOA_RELAY_TOKEN=replace-with-at-least-32-random-characters

# The container listens on 8787. This host port stays on loopback for a local
# Caddy/Nginx reverse proxy that terminates TLS.
NELOA_RELAY_PORT=8787
NELOA_RELAY_MAX_DEVICES=64
